# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: false
addons:
  apt:
    packages:
      - libudunits2-dev
cache: packages
r: release
r_github_packages:
  - r-lib/covr

after_success:
  - Rscript -e 'covr::codecov()'
